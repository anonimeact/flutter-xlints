name: xlints_example
description: Example app demonstrating widgets that trigger xlints performance rules.
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  custom_lint:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  xlints:
    path: ../
