import 'package:flutter/material.dart';

import 'lib/main.dart';

/// Minimal entrypoint that makes the published example discoverable on pub.dev.
///
/// The full demonstration app lives in `example/lib/main.dart`.
void main() => runApp(const XlintsExampleApp());
