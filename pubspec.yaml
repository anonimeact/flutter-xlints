name: xlints
description: Flutter performance linter that detects costly code patterns and provides actionable optimization guidance.
version: 1.0.1
repository: https://github.com/anonimeact/flutter-xlints.git

environment:
  sdk: ^3.10.4

dependencies:
  analyzer: ^6.0.0
  custom_lint: ^0.6.0
  custom_lint_builder: ^0.6.0

dev_dependencies:
  lints: ^6.0.0
  path: ^1.9.0
  test: ^1.25.6
